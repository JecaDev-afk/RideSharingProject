🚖 Ride-Sharing Service (ІНДЗ-2)

Прототип високонавантаженої системи замовлення таксі, реалізований на базі ASP.NET Core.
Проєкт демонструє практичну реалізацію архітектурних рішень, розроблених у рамках аналізу ATAM (ІНДЗ-1), з акцентом на масштабованість, продуктивність та роботу в реальному часі.

🏗 Архітектура та Технології

Система побудована за принципами Event-Driven Architecture для забезпечення асинхронної обробки запитів.

API Gateway (TripController): REST API, що відповідає за прийом запитів, валідацію та миттєву відповідь клієнту (202 Accepted).

Message Queue (Channel<T>): In-Memory емуляція Kafka. Використовується для буферизації запитів та згладжування пікових навантажень.

Matching Engine (MatchingEngineWorker): Фоновий процес (BackgroundService) для пошуку водія. Працює асинхронно і не блокує основний потік обробки HTTP-запитів.

Real-Time Updates (SignalR, RideHub): Забезпечує Push-повідомлення клієнтам про зміну статусу поїздки, замінюючи неефективний механізм Polling.

Geo Data Store (ConcurrentDictionary): Thread-safe сховище для швидкого доступу до координат, що емулює роботу Redis.

🚀 Як запустити проєкт

Передумови

Встановлений .NET SDK 6.0 або вище.

Інсталяція та запуск

Клонування/Відкриття папки:
Відкрийте термінал у папці з проєктом.

Відновлення пакетів:

dotnet restore



Це завантажить необхідні бібліотеки (Swashbuckle, SignalR).

Запуск сервера:

dotnet run



Доступ до додатку:
Після запуску ви побачите у консолі порти (наприклад, http://localhost:5075).

Веб-клієнт: Відкрийте http://localhost:XXXX/index.html

Swagger API: Відкрийте http://localhost:XXXX/swagger

🧪 Сценарій тестування (Demo)

Щоб перевірити роботу системи, виконайте наступні кроки на веб-сторінці:

Роль Водія:

У блоці "Панель водія" натисніть кнопку "Вийти на лінію".

Результат: Сервер зареєструє координати водія в In-Memory сховищі.

Роль Пасажира:

У блоці "Панель пасажира" натисніть "Замовити таксі".

Результат:

Статус зміниться на 🔍 ПОШУК ВОДІЯ....

Запит піде в чергу (Channel).

API миттєво поверне ID поїздки, не чекаючи завершення пошуку.

Матчинг (Matching):

Через 2-3 секунди (емуляція складності алгоритму) фоновий сервіс знайде вільного водія.

Результат: Статус автоматично зміниться на зелений: "🚗 ВОДІЯ ЗНАЙДЕНО!" завдяки SignalR.

📂 Структура проєкту

Program.cs — Вся логіка бекенду (Monolithic file pattern для простоти прототипу).

TripController — REST API.

RideHub — WebSocket логіка.

MatchingEngineWorker — Фонова логіка бізнес-процесу.

RideSharingApp.csproj — Конфігурація проєкту та залежностей.

wwwroot/index.html — Клієнтська частина (Frontend).

👨‍💻 Автор

Студент: Євгеній Ярещенко
Група: 1П-23
Дисципліна: Проєктування та реалізація сучасних вебдодатків
